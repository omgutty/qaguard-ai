We are continuing development of QAGuard AI.

IMPORTANT:
This is a production-working hackathon project. Do NOT break the existing working functionality.

We are now implementing:

PHASE 2 — STEP 5 + STEP 6 TOGETHER

Step 5:
REAL QUALITY AGENT

Step 6:
REAL AUTOMATION AGENT

==================================================
CURRENT PRODUCTION STATUS
==================================================

The following are already implemented and production verified on Vercel:

1. Requirement Agent — REAL LLM
2. Test Engine Agent — REAL LLM
3. Test Data Agent — REAL LLM
4. Human Review workflow
5. OpenRouter integration
6. DeepSeek V4 Flash
7. Server-side API key protection
8. Requirement analysis API
9. Test case generation API
10. Test data generation API
11. Workflow state persistence
12. Traceability architecture
13. Quality dashboard UI
14. Automation UI
15. GitHub repository
16. Vercel deployment

Current architecture:

Requirement
    ↓
Requirement Agent — REAL LLM
    ↓
Requirement Analysis
    ↓
Test Engine Agent — REAL LLM
    ↓
Test Cases
    ↓
Test Data Agent — REAL LLM
    ↓
Test Data
    ↓
Human Review
    ↓
Quality Agent — MOCK
    ↓
Automation Agent — MOCK

Change this to:

Requirement
    ↓
Requirement Agent — REAL LLM
    ↓
Requirement Analysis
    ↓
Test Engine Agent — REAL LLM
    ↓
Test Cases
    ↓
Test Data Agent — REAL LLM
    ↓
Test Data
    ↓
Human Review
    ↓
Quality Agent — REAL LLM
    ↓
Quality + Traceability
    ↓
Approved Test Cases
    ↓
Automation Agent — REAL LLM
    ↓
Playwright TypeScript

==================================================
IMPORTANT IMPLEMENTATION RULE
==================================================

Do NOT redesign the existing application.

Reuse the existing:

- QA types
- workflow provider
- OpenRouter provider
- validation architecture
- error handling
- UI components
- Quality page
- Automation page
- traceability utilities
- existing mock agents

Do NOT introduce:

- Langflow
- n8n
- LangChain
- AI SDK
- database
- authentication
- external infrastructure
- unnecessary npm packages

Those integrations are planned for future phases.

Do NOT modify the existing Requirement Agent, Test Engine Agent, or Test Data Agent unless required for compatibility.

==================================================
PART A — STEP 5
REAL QUALITY AGENT
==================================================

GOAL:

Replace the deterministic Quality Agent with a real LLM-powered Quality Agent.

The Quality Agent must evaluate the actual QA artifacts generated by the previous stages.

It must NOT generate arbitrary scores.

It must evaluate:

1. Requirement
2. Requirement Analysis
3. Requirement gaps
4. Requirement risks
5. Generated test cases
6. Test priorities
7. Test types
8. Test data
9. Traceability
10. Human review status

==================================================
1. INSPECT EXISTING CONTRACT
==================================================

First inspect:

src/types/qa.ts

src/agents/quality-agent.ts

src/lib/validation/

src/lib/utils/scoring.ts

src/lib/utils/traceability.ts

app/quality/page.tsx

src/lib/state/workflow-provider.tsx

Do not create duplicate types.

Preserve the existing QualityReport contract if possible.

==================================================
2. REAL QUALITY AGENT
==================================================

Modify:

src/agents/quality-agent.ts

Implement a real async LLM agent.

Expected signature should follow the existing project architecture.

Use:

generateStructuredResponse()

from the existing OpenRouter provider.

Do NOT create a new LLM client.

Create:

QUALITY_PROMPT_VERSION = "1.0"

==================================================
3. QUALITY EVALUATION PRINCIPLES
==================================================

The Quality Agent must evaluate evidence from the actual generated artifacts.

The LLM must NOT invent:

- requirements
- business rules
- test cases
- risks
- application behavior
- APIs
- UI behavior

Scores must be based on available evidence.

The Quality Agent should explicitly identify missing information instead of assuming it exists.

==================================================
4. QUALITY DIMENSIONS
==================================================

Evaluate at minimum:

Requirement Coverage
Risk Coverage
Test Coverage
Test Quality
Test Data Quality
Traceability
Overall Quality

Use the existing score fields/types if available.

Do not create duplicate scoring systems.

Scores must be:

0–100

and must be finite numeric values.

==================================================
5. QUALITY FINDINGS
==================================================

Generate useful findings such as:

- missing test coverage
- insufficient negative scenarios
- missing boundary scenarios
- requirement ambiguity
- high-risk areas without tests
- test cases without appropriate data
- missing traceability
- rejected/unreviewed tests
- duplicate or weak test scenarios

Each finding should have:

- severity
- category
- description
- evidence/source
- recommendation

Follow existing QA contracts.

==================================================
6. TRACEABILITY
==================================================

The Quality Agent must evaluate:

Requirement
    ↓
Test Case
    ↓
Test Data

and identify broken links.

Do not invent traceability.

Use existing traceability utilities and types.

==================================================
7. VALIDATION
==================================================

Create or update:

src/lib/validation/quality.ts

Follow the same hand-written validation architecture used by:

requirement.ts
analysis.ts
test-cases.ts
test-data.ts

Reject:

- invalid scores
- scores outside 0–100
- invalid enums
- malformed findings
- fabricated test case IDs
- fabricated requirement IDs
- invalid traceability
- missing required fields

Do NOT silently repair LLM output.

==================================================
8. QUALITY API
==================================================

Create:

app/api/quality/generate/route.ts

Flow:

POST
 ↓
Validate requirement
 ↓
Validate analysis
 ↓
Validate test cases
 ↓
Validate test data
 ↓
Validate review state
 ↓
Call real Quality Agent
 ↓
Validate LLM output
 ↓
Return QualityReport

Use safe error mapping.

Never expose API keys or provider internals.

==================================================
9. QUALITY UI
==================================================

Reuse:

app/quality/page.tsx

Do NOT redesign the dashboard.

The existing UI should now display real Quality Agent results.

If useful, make small additions only:

- AI-generated indicator
- evidence/source
- finding severity
- recommendation
- number of artifacts evaluated

Do not introduce a completely new design.

==================================================
10. QUALITY WORKFLOW
==================================================

Update the workflow so Quality is generated from the actual current pipeline state.

The flow should be:

Test Data
    ↓
Human Review
    ↓
Generate Quality
    ↓
Quality Report

Do not automatically approve anything.

==================================================
PART B — STEP 6
REAL AUTOMATION AGENT
==================================================

GOAL:

Replace the deterministic Automation Agent with a real LLM-powered Playwright TypeScript generation agent.

The Automation Agent should generate executable Playwright TypeScript ONLY from approved test cases.

==================================================
11. CRITICAL GOVERNANCE RULE
==================================================

The Automation Agent MUST NOT automate:

- rejected test cases
- unreviewed test cases
- test cases without approval

Only:

reviewStatus = approved

can be automated.

This is one of the core governance features of QAGuard AI.

==================================================
12. INSPECT EXISTING CONTRACT
==================================================

Inspect:

src/types/qa.ts

src/agents/automation-agent.ts

app/automation/page.tsx

src/lib/state/workflow-provider.tsx

existing code-block component

existing Playwright artifact contract

Preserve existing contracts.

==================================================
13. REAL AUTOMATION AGENT
==================================================

Implement a real async Automation Agent.

Use:

generateStructuredResponse()

from the existing OpenRouter provider.

Create:

AUTOMATION_PROMPT_VERSION = "1.0"

The model must generate Playwright TypeScript.

==================================================
14. PLAYWRIGHT REQUIREMENTS
==================================================

Generated automation should follow these principles:

- TypeScript
- Playwright
- web-first assertions
- locator-based interactions
- readable test names
- deterministic structure
- no arbitrary sleeps
- no hardcoded production credentials
- no fabricated URLs unless provided by the test case/requirement
- no invented application behavior
- no invented selectors when selectors are not available

If selectors are unavailable, clearly mark them as placeholders.

Do not pretend that a fabricated selector is verified.

==================================================
15. PAGE OBJECT SUPPORT
==================================================

If the existing AutomationArtifact contract supports page objects, use them.

If not, keep the generated test readable and compatible with the existing project.

Do not introduce a completely new framework architecture.

Generated code should be suitable for a Playwright TypeScript project.

==================================================
16. TEST DATA
==================================================

Automation should use the approved test data associated with the approved test case.

Do not generate new test data during automation.

The chain must remain:

Requirement
 ↓
Test Case
 ↓
Test Data
 ↓
Human Approval
 ↓
Automation

==================================================
17. TRACEABILITY
==================================================

Every AutomationArtifact must contain traceability to:

- requirementId
- testCaseId
- testData where supported

The user must be able to understand:

"Which requirement produced this Playwright test?"

==================================================
18. VALIDATION
==================================================

Create or update:

src/lib/validation/automation.ts

Validate:

- approved test case only
- artifact structure
- testCaseId
- requirementId
- code is non-empty
- language is TypeScript
- framework is Playwright
- traceability exists
- no fabricated IDs
- no obvious credential leakage

Reject invalid output.

Do not silently repair malformed LLM output.

==================================================
19. AUTOMATION API
==================================================

Create:

app/api/automation/generate/route.ts

Flow:

POST
 ↓
Validate approved test case
 ↓
Validate test data
 ↓
Call real Automation Agent
 ↓
Validate Playwright artifact
 ↓
Return artifact

If the test case is not approved:

return a safe 400/403-style response.

Do NOT call the LLM for unapproved tests.

==================================================
20. AUTOMATION UI
==================================================

Reuse:

app/automation/page.tsx

Do not redesign the page.

The page should:

- show approved test cases
- allow automation generation
- show generated Playwright code
- allow copy
- show traceability
- clearly identify generated code as AI-generated

If there are no approved test cases:

show a clear message such as:

"No approved test cases are available for automation."

==================================================
21. MOCK PRESERVATION
==================================================

Preserve deterministic mock implementations.

Create/use:

src/agents/quality-agent.mock.ts

src/agents/automation-agent.mock.ts

Production paths must use the real agents.

Mocks should remain available for deterministic verification.

==================================================
22. VERIFICATION SCRIPTS
==================================================

Create:

scripts/verify-quality.mts

npm run verify:quality

and:

scripts/verify-automation.mts

npm run verify:automation

Quality verification should test:

1. valid QualityReport
2. invalid score
3. score >100
4. malformed finding
5. invalid severity
6. fabricated test case ID
7. invalid traceability
8. missing required field
9. missing API key
10. provider error
11. mock shape

Automation verification should test:

1. approved test case accepted
2. rejected test case rejected
3. unreviewed test case rejected
4. missing test case rejected
5. fabricated requirement ID rejected
6. malformed artifact rejected
7. missing code rejected
8. invalid language rejected
9. invalid framework rejected
10. credential leakage rejected
11. missing API key
12. provider error
13. mock shape

Also run all existing regression checks:

npm run verify:agent
npm run verify:engine
npm run verify:test-data

==================================================
23. REAL LLM TEST
==================================================

Perform real local or Vercel testing where possible.

Because the local machine has a known TLS CA problem, do NOT modify production code to bypass TLS.

If local OpenRouter calls fail with:

UNABLE_TO_GET_ISSUER_CERT_LOCALLY

record this as a local environment limitation.

Use Vercel production for the final real LLM verification.

Verify:

Requirement Agent → REAL
Test Engine Agent → REAL
Test Data Agent → REAL
Quality Agent → REAL
Automation Agent → REAL

DeepSeek V4 Flash should be confirmed through OpenRouter usage.

==================================================
24. END-TO-END PIPELINE
==================================================

The final pipeline should work as:

Requirement
   ↓
Requirement Analysis
   ↓
Test Generation
   ↓
Test Data
   ↓
Human Review
   ↓
Quality Assessment
   ↓
Approved Test Cases
   ↓
Playwright Generation

The Quality Agent must evaluate the pipeline before automation.

Automation must only use approved test cases.

==================================================
25. SECURITY
==================================================

Verify:

- .env.local remains gitignored
- no NEXT_PUBLIC_OPENROUTER_API_KEY
- no API key in source
- no credentials in test fixtures
- no API keys in generated Playwright
- no secrets logged
- OpenRouter remains server-only
- safe API errors
- no client-side provider calls

==================================================
26. REGRESSION
==================================================

Run:

npm run lint

npm run build

npm run verify:agent

npm run verify:engine

npm run verify:test-data

npm run verify:quality

npm run verify:automation

Verify all existing pages:

/
 /requirements
 /test-cases
 /test-data
 /review
 /quality
 /automation

Do not break existing functionality.

==================================================
27. IMPORTANT — NO GIT / VERCEL
==================================================

Do NOT:

git commit
git push
force push
deploy to Vercel

I will review first.

==================================================
28. FINAL REPORT
==================================================

When complete, report:

1. files created
2. files modified
3. Quality Agent architecture
4. Quality prompt design
5. Quality validation
6. Quality scoring approach
7. traceability implementation
8. Automation Agent architecture
9. Automation prompt design
10. Playwright generation rules
11. approval governance
12. Automation validation
13. mock preservation
14. API routes created
15. UI changes
16. verification results
17. lint result
18. build result
19. real LLM test result
20. DeepSeek confirmation
21. whether Quality used real LLM
22. whether Automation used real LLM
23. number of generated findings
24. number of approved tests automated
25. any issues/deviations

STOP after Phase 2 Step 6.

Do not start Jira integration, RAG, Langflow, MCP, CI/CD, or other future phases.